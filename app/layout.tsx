import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "API-Cert | Healthcare License Verification API",
  description: "Verify RN, LPN, NP, PA, MD, and DO licenses across all 50 states in under 100ms. Official government data sources, updated daily.",
  keywords: "healthcare license verification, RN verification, nursing license API, medical license check, NPPES, OIG exclusion check",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
